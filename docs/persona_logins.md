# Persona logins — the seeded verification world

_GENERATED from `digi-tax-backend/app/cli/world_fixtures.py` — do not edit by
hand; regenerate after any seed change (commands in that module's docstring)._

## How to log in

ورود با کد یکبارمصرف: شماره را وارد کنید، «دریافت کد تأیید» را بزنید — در محیط توسعه کد ۶رقمی همان‌جا زیر فرم نمایش داده می‌شود (dev_otp)؛ همان را وارد کنید. کاربرانی که رمز دارند می‌توانند با «ورود با رمز عبور» و رمز Admin@12345 هم وارد شوند.

| موبایل | نقش | کسب‌وکارها | چه چیزی را ببینید | ورود |
|---|---|---|---|---|
| `09120000001` | system_admin | — | پنل مدیریت: داشبورد (شمارنده‌ها)، بررسی پرونده‌ها، همکاران، فعال‌سازی‌ها، تقویم مالیاتی | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001001` | merchant_owner | کافه دنج | داشبورد عملیاتی، کالا/خدمات با ستون موجودی (انبار ساده)، فاکتورها، چک‌ها | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001002` | merchant_owner | بازرگانی نیک‌تجارت | نمای حسابدار (دفتر روزنامه/کل/تراز)، گزارش سود و زیان، دانلود اکسل — دنیای مدرسهٔ حسابداری | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001003` | merchant_owner | پخش آریا، فروشگاه آریا سنتر، خدمات آریا تک | سوئیچر ۳ کسب‌وکار (اعداد داشبورد هر کدام متفاوت)، اعضای تیم روی پخش آریا | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001006` | member_admin | پخش آریا (مدیر) | دسترسی مدیر: همهٔ ۲۰ فاکتور پخش آریا را می‌بیند | فقط کد یکبارمصرف |
| `09120001007` | member_staff | پخش آریا (کارمند) | دسترسی کارمند: فقط ۷ فاکتور خودش را می‌بیند (own-only) | فقط کد یکبارمصرف |
| `09120001004` | partner_approved | — | پنل همکار (داخل /admin): مشتریان من (۲ کسب‌وکار)، درآمد و تسویه، کد همکار HAM-TEST1 | فقط کد یکبارمصرف |
| `09120001005` | partner_pending | — | وضعیت «در حال بررسی» درخواست همکاری | فقط کد یکبارمصرف |

## Expected headline numbers (asserted by the experience harness)

- کافه دنج: ۳۰ کالا (۱۰ ردیابی‌شده)، «کیک شکلاتی 1» با موجودی **−۲**، ۴ کالای رو به اتمام، ۶۰ فاکتور نهایی.
- نیک‌تجارت: سود **۱۶۵٬۰۰۰٬۰۰۰ ریال**، صندوق ۱۳۷M، بانک ملت ۱٬۰۱۶M، تنخواه ۸M — تراز همیشه برقرار.
- هلدینگ آریا: ۳ کسب‌وکار با ۲۰/۳۰/۴۰ فاکتور؛ کارمند فقط ۷ فاکتور خودش، مدیر همهٔ ۲۰.
- خانم محمدی: ۲ مشتری در پرتفوی، کمیسیون ۱۵٪، معوقهٔ ۱٬۸۰۰٬۰۰۰؛ فعال‌سازی اعتباری انبار ساده (تسویه‌نشده).
- ادمین: شمارنده‌های داشبورد ۱/۱/۱/۱/۱ (پرونده/همکار/درخواست/تسویه)، تمدید تقویم مالیاتی 1405Q1.

