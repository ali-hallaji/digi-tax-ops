# Persona logins — the seeded verification world

_GENERATED from `digi-tax-backend/app/cli/world_fixtures.py` — do not edit by
hand; regenerate after any seed change (commands in that module's docstring)._

## How to log in

ورود با کد یکبارمصرف: شماره را وارد کنید، «دریافت کد تأیید» را بزنید — در محیط توسعه کد ۶رقمی همان‌جا زیر فرم نمایش داده می‌شود (dev_otp)؛ همان را وارد کنید. کاربرانی که رمز دارند می‌توانند با «ورود با رمز عبور» و رمز Admin@12345 هم وارد شوند.

| موبایل | نقش | کسب‌وکارها | چه چیزی را ببینید | ورود |
|---|---|---|---|---|
| `09120000001` | system_admin | — | پنل مدیریت: داشبورد (شمارنده‌ها)، بررسی پرونده‌ها، همکاران، فعال‌سازی‌ها، تقویم مالیاتی | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001001` | merchant_owner | کافه دنج | داشبورد عملیاتی، کالا/خدمات با ستون موجودی (انبار ساده)، فاکتورها، چک‌ها | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001002` | merchant_owner | بازرگانی نیک‌تجارت | نمای حسابدار (دفتر روزنامه/کل/تراز)، گزارش سود و زیان، دانلود اکسل — دنیای مدرسهٔ حسابداری | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001003` | merchant_owner | پخش آریا، فروشگاه آریا سنتر، خدمات آریا تک | سوئیچر ۳ کسب‌وکار (اعداد داشبورد هر کدام متفاوت)، اعضای تیم روی پخش آریا | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001006` | member_admin | پخش آریا (مدیر) | دسترسی مدیر: همهٔ ۲۰ فاکتور پخش آریا را می‌بیند | فقط کد یکبارمصرف |
| `09120001007` | member_staff | پخش آریا (کارمند) | دسترسی کارمند: فقط ۷ فاکتور خودش را می‌بیند (own-only) | فقط کد یکبارمصرف |
| `09120001004` | partner_approved | — | پنل همکار (داخل /admin): مشتریان من (۳ کسب‌وکار)، درآمد و تسویه، کد همکار HAM-TEST1 | فقط کد یکبارمصرف |
| `09120001005` | partner_pending | — | وضعیت «در حال بررسی» درخواست همکاری | فقط کد یکبارمصرف |
| `09120001008` | merchant_owner | سوپرمارکت محله | حجم بالا: ۲۰۰ مشتری، ۴۰ کالا با انبار ساده، ۵۰۰ فاکتور نهایی، ۳۰ چک | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001009` | merchant_owner | آژانس خدماتی مهر | کسب‌وکار خدماتی محض (بدون انبار)، سال مالی و واحد پول متفاوت (تومان)، پیش‌دریافت‌های سنگین | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001010` | merchant_owner | شرکت بازرگانی دوم | کسب‌وکار کوچک و مستقل — سومین مشتریِ اعطاشده (نه ارجاع‌شده) در پرتفوی خانم محمدی (p4) | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001011` | partner_approved | — | همکار تأییدشدهٔ دوم (کد HAM-TEST2)، پرتفوی مستقل خودش با کمیسیون ۲۰٪ | رمز Admin@12345 یا کد یکبارمصرف |
| `09120001012` | partner_and_merchant_owner | دفتر سمیرا | مسیر دوگانه: هم همکار تأییدشده (کد HAM-TEST3) و هم صاحب کسب‌وکار شخصی خودش | رمز Admin@12345 یا کد یکبارمصرف |

## چه چیزی را ببین (چک‌لیست بازبینیِ هدایت‌شده)

برای هر پرسونا وارد شو و این چند مورد مشخص را بررسی کن:

### DigiTax Admin — `09120000001` (system_admin · —)
- داشبورد ادمین را باز کن — شمارنده‌ها باید ۲ پروندهٔ در انتظار، ۱ همکار در انتظار، ۱ درخواست ماژول، ۱ فعال‌سازی اعتباری، ۱۴ کاربر و ۱۰ کسب‌وکار نشان دهند.
- «همکاران» را باز کن — زهرا محمدی (تأییدشده) و پویا اسدی (در انتظار) را ببین.
- «فعال‌سازی‌ها و درخواست‌ها» را باز کن — فعال‌سازی اعتباری تسویه‌نشدهٔ «انبار ساده» روی پخش آریا را ببین.
- «تقویم مالیاتی» را باز کن — باید بتوانی مهلت ۱۴۰۵Q1 را تمدید کنی.

### سارا کریمی — `09120001001` (merchant_owner · کافه دنج)
- «کالا و خدمات» را باز کن — ۳۰ کالا؛ ستون موجودی برای ۱۰ کالای ردیابی‌شده، و «کیک شکلاتی 1» با موجودی ۲− (قرمز).
- داشبورد را باز کن — کارت «۴ کالای رو به اتمام» را ببین.
- «فاکتورها» را باز کن — ۶۰ فاکتور نهایی.
- «مشتریان» را باز کن — ۱۵ مشتری.

### محمد توکلی — `09120001002` (merchant_owner · بازرگانی نیک‌تجارت)
- «تنظیمات» → نمای حسابدار را روشن کن — دفتر روزنامه/کل/تراز باید ظاهر شوند.
- «سود و زیان» را باز کن — سود خالص باید ۱۶۵٬۰۰۰٬۰۰۰ ریال باشد.
- «تراز آزمایشی» را باز کن — صندوق ۱۳۷M، بانک ملت ۱٬۰۱۶M، تنخواه ۸M؛ تراز باید برقرار باشد.
- در دفتر روزنامه، سند دستیِ «سند افتتاحیهٔ نمونه (نیک‌تجارت)» را ببین.

### آریا نجفی — `09120001003` (merchant_owner · پخش آریا، فروشگاه آریا سنتر، خدمات آریا تک)
- سوییچر کسب‌وکار را باز کن — سه کسب‌وکار: پخش آریا، فروشگاه آریا سنتر، خدمات آریا تک.
- روی هر کسب‌وکار «فاکتورها» را باز کن — به‌ترتیب ۲۰، ۳۰ و ۴۰ فاکتور.
- روی پخش آریا «اعضای تیم» را باز کن — کامران سعیدی (مدیر) و نیلوفر رستمی (کارمند).

### کامران سعیدی — `09120001006` (member_admin · پخش آریا (مدیر))
- «فاکتورهای» پخش آریا را باز کن — همهٔ ۲۰ فاکتور دیده می‌شوند (دسترسی مدیر).
- فقط عضو پخش آریا هستی — کسب‌وکار دیگری در سوییچر نداری.

### نیلوفر رستمی — `09120001007` (member_staff · پخش آریا (کارمند))
- «فاکتورهای» پخش آریا را باز کن — فقط ۷ فاکتور خودت (دسترسی کارمند، own-only).
- فاکتورهای ساخته‌شده توسط دیگران را نباید ببینی.

### زهرا محمدی — `09120001004` (partner_approved · —)
- بعد از ورود مستقیم روی «مشتریان من» (/admin/my-clients) فرود می‌آیی — نه ویزارد کاسب.
- پرتفوی باید دقیقاً ۳ مشتری داشته باشد: بازرگانی نیک‌تجارت، پخش آریا و شرکت بازرگانی دوم (این سومی فقط اعطاشده است — نه ارجاع‌شده، برخلاف دو مورد اول).
- «درآمد و تسویه» را باز کن — کمیسیون ۱۵٪ و معوقهٔ ۱٬۸۰۰٬۰۰۰ را ببین.
- /app را دستی در نوار آدرس باز کن — باید دوباره به «مشتریان من» برگردی؛ هیچ «افزودن کسب‌وکار» و هیچ «پنل کسب‌وکار» در منوها نیست. ساخت کسب‌وکار شخصی فقط از «پروفایل و کد همکار» → بخش «کسب‌وکار شخصی».

### پویا اسدی — `09120001005` (partner_pending · —)
- بعد از ورود، صفحهٔ «درخواست شما در حال بررسی است» را می‌بینی.
- تا وقتی ادمین تأیید نکرده، به پنل همکار (مشتریان/درآمد) دسترسی نداری.

### مهدی اکبری — `09120001008` (merchant_owner · سوپرمارکت محله)
- «کالا و خدمات» را باز کن — ۴۰ کالا؛ ستون موجودی برای ۳۴ کالای ردیابی‌شده.
- «فاکتورها» را باز کن — ۵۰۰ فاکتور نهایی، ترکیبی از تسویه‌شده و نسیه.
- «مشتریان» را باز کن — ۲۰۰ مشتری.
- «چک‌ها» را باز کن — ۳۰ چک؛ ۱۰ وصول‌شده (کلیر) و ۲۰ در جریان وصول.

### لیلا نوری — `09120001009` (merchant_owner · آژانس خدماتی مهر)
- «کالا و خدمات» را باز کن — ۱۵ خدمت؛ هیچ ستون موجودی‌ای نیست (بدون انبار ساده).
- «فاکتورها» را باز کن — ۸۰ فاکتور خدماتی نهایی.
- «مشتریان» را باز کن — ۴۰ مشتری.
- «تراکنش‌ها/پرداخت‌ها» را باز کن — ۳۰ پیش‌دریافت (بدون فاکتور) را ببین.

### رضا کریمی — `09120001010` (merchant_owner · شرکت بازرگانی دوم)
- کسب‌وکاری کوچک با ۵ مشتری، ۵ کالا و ۸ فاکتور (ترکیبی از تسویه‌شده و نسیه).
- با ورود p4 و باز کردن «مشتریان من»، این کسب‌وکار در کنار بازرگانی نیک‌تجارت و پخش آریا دیده می‌شود.

### آرش رستمی — `09120001011` (partner_approved · —)
- بعد از ورود روی «مشتریان من» فرود می‌آیی — پرتفوی باید دقیقاً ۱ مشتری داشته باشد: فروشگاه رستمی.
- «درآمد و تسویه» را باز کن — کمیسیون ۲۰٪ و تسویهٔ ۲٬۰۰۰٬۰۰۰ را ببین (متفاوت از p4).

### سمیرا حسینی — `09120001012` (partner_and_merchant_owner · دفتر سمیرا)
- بعد از ورود روی «مشتریان من» فرود می‌آیی (پنل همکار) — پرتفوی خالی است (هنوز مشتری‌ای اعطا نشده).
- از «پروفایل و کد همکار» → بخش «کسب‌وکار شخصی» وارد «دفتر سمیرا» شو — ۸ خدمت و ۱۲ فاکتور را ببین.


## Expected headline numbers (asserted by the experience harness)

- کافه دنج: ۳۰ کالا (۱۰ ردیابی‌شده)، «کیک شکلاتی 1» با موجودی **−۲**، ۴ کالای رو به اتمام، ۶۰ فاکتور نهایی.
- نیک‌تجارت: سود **۱۶۵٬۰۰۰٬۰۰۰ ریال**، صندوق ۱۳۷M، بانک ملت ۱٬۰۱۶M، تنخواه ۸M — تراز همیشه برقرار.
- هلدینگ آریا: ۳ کسب‌وکار با ۲۰/۳۰/۴۰ فاکتور؛ کارمند فقط ۷ فاکتور خودش، مدیر همهٔ ۲۰.
- خانم محمدی: ۳ مشتری در پرتفوی (۲ ارجاع‌شده + ۱ فقط‌اعطاشده)، کمیسیون ۱۵٪، معوقهٔ ۱٬۸۰۰٬۰۰۰؛ فعال‌سازی اعتباری انبار ساده (تسویه‌نشده).
- سوپرمارکت محله: ۲۰۰ مشتری، ۴۰ کالا (۳۴ ردیابی‌شده)، ۵۰۰ فاکتور نهایی، ۳۰ چک (۱۰ کلیرشده + ۲۰ در جریان).
- آژانس خدماتی مهر: فقط خدمات (بدون انبار)، ۴۰ مشتری، ۱۵ خدمت، ۸۰ فاکتور، ۳۰ پیش‌دریافت سنگین؛ سال مالی ۰۷-۰۱ و واحد پول تومان.
- شرکت بازرگانی دوم: کسب‌وکار کوچک ۵ مشتری/۵ کالا/۸ فاکتور — سومین مشتریِ اعطاشده در پرتفوی خانم محمدی.
- آرش رستمی: همکار دوم (HAM-TEST2)، ۱ مشتری در پرتفوی (فروشگاه رستمی)، کمیسیون ۲۰٪، تسویهٔ ۲٬۰۰۰٬۰۰۰.
- سمیرا حسینی: همکار سوم (HAM-TEST3) و هم‌زمان صاحب کسب‌وکار شخصی «دفتر سمیرا» (۸ خدمت، ۱۲ فاکتور) — مسیر دوگانه.
- ادمین: شمارنده‌های داشبورد ۱/۱/۱/۱/۱ (پرونده/همکار/درخواست/تسویه) — نیازمند بازبینی پس از گسترش دنیا به ۱۳ پرسونا؛ تمدید تقویم مالیاتی 1405Q1.

